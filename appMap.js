// appMap.js — מפה מלאה ומתעדכנת של האפליקציה, שמוזרקת לזיכרון של איריס (מעצבת המוצר)
// בכל שיחה. מורכבת משני חלקים:
//   1) תיאור מבני/פיצ'רים (מתוחזק בקוד — לעדכן כשמוסיפים פיצ'ר).
//   2) נתונים חיים מתוך ה-DB (עסקים, כמויות, חיבורים) — מתעדכנים אוטומטית בכל קריאה.
// כך שהמפה שאיריס רואה תמיד משקפת את המצב הנוכחי של המערכת, כולל שינויים.

// רשימת הלשוניות ותיאורן (מקור אמת יחיד לתיאורים)
export const TAB_MAP = [
  ['home', '🏠 בית', 'סקירה חודשית מחשבונית ירוקה: הכנסות/הוצאות, חשבוניות פתוחות מקובצות לפי לקוח, הפקת מסמכי המשך ושכפול.'],
  ['events', 'אירועים ויומן', 'קליטת אירועים (הופעות) מווטסאפ ומיומן גוגל, התאמה בין המקורות, לוח שנה חודשי/שבועי, עריכת אירוע מלאה (תבנית, 3 מחירים, לקוח, עובדים, קבלנים, תמחור סאונד/בקליין/תאורה/מסך לד), שיוך אירועים לחשבונית.'],
  ['clients', 'לקוחות', 'רשימת לקוחות מחשבונית ירוקה, כרטיס לקוח עם מסמכים, חיפוש מסמכים, הפקת חשבונית מאירועים, קישור אירועים לחשבונית קיימת, פעולות מסמך (סגירה/פתיחה/זיכוי).'],
  ['invoicing', '🧾 חשבוניות', 'הפקת חשבוניות (עסקה 300, מס 305, מס-קבלה 320, קבלה 400), תצוגה מקדימה מעוצבת לפני הפקה, שליחת מייל ללקוח (עם אישור), בורר תאריך עם אישור תאריך-אחורה.'],
  ['quotes', '📄 הצעות מחיר', 'רשימת הצעות מחיר פתוחות, יצירת הצעה חדשה מאפס, שכפול הצעה עם עריכה, מסמך המשך, סגירה.'],
  ['contractors', 'קבלנים', 'רשימת ספקים/קבלנים עם כרטיס מסמכים; רשימת ספקים לתשלום לפי הכנסת אירועים; רשימת ספקים לתשלום; קליטת חשבונית ספק (הוצאה) עם AI ל-PDF/תמונה, מספר הקצאה, סיווג חשבונאי; קישור אירועי קבלן לחשבונית ספק; עריכת תיאור הוצאה (מתעדכן בחשבונית ירוקה ובהתאמות הבנק); כפתור "טופל".'],
  ['payroll', 'עובדים', 'עובדים (מידע פנימי), חישוב שכר בסיס×פקטור + בונוסים, שורות עובד לכל אירוע, טבלת עובד חודשית בפורמט גיליון + ייצוא CSV.'],
  ['bank', '🏦 בנק', 'העלאת קובץ תנועות בנק (מזרחי), יתרת עו"ש מכותרת הקובץ, התאמה אוטומטית של תנועות זכות לחשבוניות פתוחות ותנועות חובה לחשבוניות ספק (התאמה מדויקת / פחות 5% ניכוי מס במקור / לפי מספר מסמך / שם / תאריך), שיוך ידני ("שייך"), כפתור "צור הכנסה" ליצירת מס-קבלה/קבלה מהתאמה, מסננים לפי זכות/חובה/הכל ולפי תקופה.'],
  ['devrequests', '🛠️ בקשות פיתוח', 'בקשות הפיתוח שנוצרות מסיכום שיחות עם הצוות (איריס). לכל בקשה סטטוס: חדש → מטופל → טופל. ההנהלה מעדכנת סטטוס.'],
  ['team', '👥 הצוות', 'צ\'אט עם דמויות AI של הצוות (כרגע: איריס — מעצבת מוצר). זיכרון מתמשך לכל דמות, וכפתור "סכם כבקשת פיתוח" שהופך שיחה לבקשה בלשונית בקשות הפיתוח.'],
  ['connections', '🔌 חיבורים', 'מרכז חיבורים: חשבונית ירוקה (Morning), יומן גוגל, גשר ווטסאפ, מפתחות AI — עם סטטוס ותאריך התחברות.'],
];

// סוגי מסמכים בחשבונית ירוקה
const DOC_TYPES = '10=הצעת מחיר, 20=חשבון עסקה (רישום פנימי), 300=חשבון עסקה, 305=חשבונית מס, 320=חשבונית מס-קבלה, 330=זיכוי, 400=קבלה. מע"מ 18%. ניכוי מס במקור נפוץ 5% (מכפיל 0.95).';

// שפת העיצוב הקיימת (כדי שאיריס תעצב בהתאמה)
const DESIGN_SYSTEM = `RTL מלא, עברית. פלטה בהירה: אינדיגו כצבע ראשי (--primary) וענבר/כתום כהדגשה (--warn). ירוק (--accent2) להצלחות, אדום (--danger) לסכנות. משתני CSS מרכזיים: --primary, --accent2, --warn, --danger, --panel, --panel2, --line, --muted, --grad-soft. רכיבים: .panel (כרטיס), .btn (primary/success/danger/ghost), .tag (pending/invoiced/match/miss), .card, .row-between. עיצוב עסקי-מודרני ולא צעקני.`;

function n(v) { return Array.isArray(v) ? v.length : (v == null ? 0 : v); }

export function buildAppMap(db = {}) {
  const companies = (db.companies || []).map(c => `${c.name}${c.active ? '' : ' (לא פעיל)'}`).join(', ') || '—';
  const conns = db.connections || {};
  const connLine = ['greenInvoice', 'googleCalendar']
    .map(k => `${k === 'greenInvoice' ? 'חשבונית ירוקה' : 'יומן גוגל'}: ${(conns[k] && conns[k].status) === 'connected' ? 'מחובר' : 'לא מחובר'}`)
    .join(' · ');
  const tabs = TAB_MAP.map(([, label, desc]) => `• ${label} — ${desc}`).join('\n');

  return `# מפת האפליקציה — מערכת א.ש ניהול פיננסי (מעודכן אוטומטית, ${new Date().toLocaleDateString('he-IL')})
מערכת ניהול פיננסי/תפעולי לחברות הגברה ותאורה (הפקת הופעות חיות). בנויה SPA פשוט (HTML/CSS/JS ללא ספריות), עברית ו-RTL, מתארחת ב-Render עם נתונים ב-Postgres.

## עסקים במערכת
${companies}

## אינטגרציות
חשבונית ירוקה (Morning) להפקת מסמכים והוצאות; יומן גוגל לאירועים; גשר ווטסאפ לקליטת אירועים; AI (Claude/Gemini) לקליטת חשבוניות ולצ'אט הצוות.
מצב חיבורים נוכחי: ${connLine}

## הלשוניות והמסכים
${tabs}

## סוגי מסמכים (חשבונית ירוקה)
${DOC_TYPES}

## הרשאות
משתמש הנהלה (admin) רואה הכל. משתמשי צפייה (viewer) רואים רק לשוניות/עסקים מסוימים, בקריאה בלבד.

## שפת העיצוב הקיימת
${DESIGN_SYSTEM}

## נתונים חיים (עכשיו)
אירועים: ${n(db.events)} · לקוחות/ספקים: ${n(db.contractors)} · עובדים: ${n(db.employees)} · חשבוניות שהופקו: ${n(db.invoices)} · תנועות בנק: ${n(db.bankTx)} · בקשות פיתוח פתוחות: ${(db.requests || []).filter(r => r.status !== 'done').length}.

(המפה הזו נבנית מחדש בכל שיחה מתוך הקוד והנתונים החיים, כך שהיא תמיד עדכנית. השתמשי בה כדי להבין את המערכת ולתת המלצות עיצוב קונקרטיות וברות-יישום.)`;
}

export default { buildAppMap, TAB_MAP };
